# Local Prisma database (SQLite). For PostgreSQL, see docker-compose.yml
DATABASE_URL="file:./dev.db"

# Session signing secret — change in production
AUTH_SECRET="change-this-to-a-long-random-string"

# Public origin used for class joining links
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Existing public-site admin bootstrap (academy JSON store)
ACADEMY_ADMIN_EMAIL="admin@alvasatiya.org"
ACADEMY_ADMIN_PASSWORD="AlvasatiyaAdmin!2026"
ACADEMY_TEACHER_PASSWORD="AlvasatiyaTeacher!2026"
